# state space?
c = 3 # num_connections per station
s = 61 # num_stations
m = 10 # max connections per trein
sum = 0
for i in range(1,m):
    # print(sum)
    # print(pow(c,i)*s/2)
    sum += pow(c,i)*s
print('{:e}'.format(sum))